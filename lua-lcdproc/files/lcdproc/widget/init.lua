local p = "lcdproc.widget."

return {
  string   = require (p .. "string"),
  title    = require (p .. "title"),
  hbar     = require (p .. "hbar"),
  vbar     = require (p .. "vbar"),
  icon     = require (p .. "icon"),
  scroller = require (p .. "scroller"),
  frame    = require (p .. "frame"),
  number   = require (p .. "number")
}
